{
  "name": "rye-demo-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile wrapper for rye WASM demo — runs in WebView on Android/iOS",
  "scripts": {
    "build:wasm": "cd .. && wasm-pack build --target web --out-dir www/pkg --no-opt",
    "sync": "npm run build:wasm && npx cap sync",
    "sync:android": "npm run build:wasm && npx cap sync android",
    "sync:ios": "npm run build:wasm && npx cap sync ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "run:android": "npx cap run android",
    "run:ios": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^7.0.2"
  }
}
