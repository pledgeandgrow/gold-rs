[package]
name = "rye-serialize"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal serializer for rye — SSR state transfer without serde overhead"

[dependencies]
