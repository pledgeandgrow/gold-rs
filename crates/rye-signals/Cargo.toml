[package]
name = "rye-signals"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Fine-grained reactive signals for rye — Signal, Memo, Effect, Resource, GlobalSignal"

[dependencies]
